---
# =============================================================================
#  deploy-ubuntu.yml  —  Deploy all agents to Ubuntu targets (BT1 + BT2)
#  Usage:  ansible-playbook deploy-ubuntu.yml
# =============================================================================

- name: Deploy all agents — Ubuntu (all blue teams)
  hosts: ubuntu
  gather_facts: true
  roles:
    - realm
    - sliver
    - caldera
