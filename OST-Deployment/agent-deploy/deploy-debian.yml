---
# =============================================================================
#  deploy-debian.yml  —  Deploy Realm + Sliver + Caldera to Debian 13 only
#  Usage:  ansible-playbook deploy-debian.yml
# =============================================================================

- name: Deploy all agents — Debian
  hosts: debian
  gather_facts: true
  roles:
    - realm
    - sliver
    - caldera
