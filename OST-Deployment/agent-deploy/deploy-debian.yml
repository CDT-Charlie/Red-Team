---
# =============================================================================
#  deploy-debian.yml  —  Deploy all agents to Debian targets (BT1 + BT2)
#  Usage:  ansible-playbook deploy-debian.yml
# =============================================================================

- name: Deploy all agents — Debian (all blue teams)
  hosts: debian
  gather_facts: true
  roles:
    - realm
    - sliver
    - caldera
