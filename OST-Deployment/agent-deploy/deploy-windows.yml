---
# =============================================================================
#  deploy-windows.yml  —  Deploy Realm + Sliver + Caldera to Windows Server 2022 only
#  Usage:  ansible-playbook deploy-windows.yml
# =============================================================================

- name: Deploy all agents — Windows
  hosts: windows
  gather_facts: true
  roles:
    - realm
    - sliver
    - caldera
