---
# =============================================================================
#  deploy-windows.yml  —  Deploy all agents to ALL Windows targets (BT1 + BT2)
#  Usage:  ansible-playbook deploy-windows.yml
# =============================================================================

- name: Deploy all agents — Windows (all blue teams)
  hosts: windows
  gather_facts: true
  roles:
    - realm
    - sliver
    - caldera
